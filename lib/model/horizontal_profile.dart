import 'package:flutter/cupertino.dart';

class ListHorizontalProfile {
  String title;
  IconData icon;
  String image;
  Widget widget;

  ListHorizontalProfile(this.title, this.icon, this.widget, {this.image = ''});
}

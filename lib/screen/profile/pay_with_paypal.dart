import 'package:flutter/material.dart';

class PayWithVisa extends StatelessWidget {
  const PayWithVisa({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

import 'package:flutter/material.dart';

class EditMyProjectScreen extends StatelessWidget {
  const EditMyProjectScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
